using UnityEngine;
using System.Collections;
using Vuforia  ;

public class TargetsIniter : MonoBehaviour {

	public string controllerName ;
//	public Ctrl_bake controller ; 
	// Use this for initialization
	void Awake (){
//		controller = GameObject.Find(controllerName).GetComponent<Ctrl_bake>();
	}

	void Start () {
//		RadianTrackableEventHandler rte = GetComponentsInChildren<RadianTrackableEventHandler>();   
//		foreach(RadianTrackableEventHandler r in rte ) {
//			r.onFound.AddListener(controller.Answer,) ; 
//		}
		
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
